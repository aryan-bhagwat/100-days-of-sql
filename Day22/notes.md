## Day 22 – Top Customers by Orders

**Challenge:** Find the 5 customers with the most orders.

**Learned:**
- `GROUP BY` + `COUNT()` to aggregate
- `ORDER BY DESC` + `LIMIT` to get top N
- Common in customer segmentation
